# Relatórios temporários de auditoria de performance
scripts/lh-*.json

# Dependências locais (preview dev — npx serve)
node_modules/
package.json
package-lock.json

# Config local do Claude Code
.claude/

# Contexto interno do projeto (não deve ficar público via GitHub Pages)
CLAUDE.md

# email-templates/ inteiro sai do repo público (migração pra S3 privado,
# 01/07/2026). Os templates de campanha têm preço/data/estratégia de
# lançamento e ficavam expostos via GitHub Pages semanas antes do envio.
# Agora vivem no repo privado gruponbeducacao/fluencia-email-templates e são
# servidos ao motor SES via S3 privado (SigV4), NÃO mais por HTTP público.
# Ver _CRM/runbook-templates-s3-privado.md. A pasta continua no disco local
# (fora do git) só pra referência; a fonte da verdade é o repo privado.
email-templates/

# Arte bruta / drafts (não versionados)
artes/
